# Copy to .kamal/secrets, fill in placeholder, never commit.
# This service only needs registry credentials — no Rails master key, no DB
# URL, no app secrets. The frontend container only serves static files and
# reverse-proxies to the API.

KAMAL_REGISTRY_PASSWORD=REPLACE_WITH_DOCKERHUB_ACCESS_TOKEN
